import { log, warn, info, table } from './consoleWrapper';

// eslint-disable-next-line import/prefer-default-export
export const consoleWrapper = { log, warn, info, table };
export fetchRequest from './fetchRequest';
